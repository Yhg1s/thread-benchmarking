# Generated file: !!! DO NOT EDIT !!!
---
name: _benchmark

on:
  workflow_call:
    inputs:
      fork:
        description: Fork of cpython to benchmark
        type: string
      ref:
        description: Branch, tag or (full) SHA commit to benchmark
        type: string
      machine:
        description: Machine to run on
        type: string
      benchmarks:
        description: Benchmarks to run (comma-separated; empty runs all 
          benchmarks)
        type: string
      pgo:
        description: Build with PGO
        type: boolean
      force:
        description: Rerun and replace results if commit already exists
        type: boolean
      perf:
        description: Collect Linux perf profiling data (Linux only)
        type: boolean

      tier2:
        description: tier 2 interpreter
        type: boolean
        default: false
      jit:
        description: JIT
        type: boolean
        default: false
      nogil:
        description: free threading
        type: boolean
        default: false
      tailcall:
        description: tail-calling interpreter
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      fork:
        description: Fork of cpython to benchmark
        type: string
        default: python
      ref:
        description: Branch, tag or (full) SHA commit to benchmark
        type: string
        default: main
      machine:
        description: Machine to run on
        default: linux-amd64
        type: choice
        options:
        - linux-x86_64-centurion_clang22
        - linux-x86_64-centurion_gcc15
        - linux-x86_64-centurion_gcc9
        - all
      benchmarks:
        description: Benchmarks to run (comma-separated; empty runs all 
          benchmarks)
        type: string
      force:
        description: Rerun and replace results if commit already exists
        type: boolean
      perf:
        description: Collect Linux perf profiling data (Linux only)
        type: boolean

      tier2:
        description: tier 2 interpreter
        type: boolean
        default: false
      jit:
        description: JIT
        type: boolean
        default: false
      nogil:
        description: free threading
        type: boolean
        default: false
      tailcall:
        description: tail-calling interpreter
        type: boolean
        default: false
jobs:
  benchmark-linux-x86_64-centurion_clang22:
    runs-on: [self-hosted, linux, bare-metal, centurion]
    timeout-minutes: 1440

    steps:
    - name: Setup environment
      run: |-
        echo "PYTHONHASHSEED=42" >> $GITHUB_ENV
        echo "LDFLAGS=-fuse-ld=lld" >> $GITHUB_ENV
        echo "CC=clang-22" >> $GITHUB_ENV
        echo "CXX=clang++-22" >> $GITHUB_ENV
        echo "LLVM_AR=/usr/bin/llvm-ar-22" >> $GITHUB_ENV
        echo "LLVM_PROFDATA=/usr/bin/llvm-profdata-22" >> $GITHUB_ENV
        echo "CPU_AFFINITY=12,13,14,15,16,17,18,19" >> $GITHUB_ENV
        echo "BENCHMARK_MACHINE_NICKNAME=centurion_clang22" >> $GITHUB_ENV
        echo "BENCHMARK_RUNNER_NAME=linux-x86_64-centurion_clang22" >> $GITHUB_ENV
    - name: Checkout benchmarking
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd   # v6.0.2
    - name: git gc
      run: |
        git gc
    - uses: fregante/setup-git-user@024bc0b8e177d7e77203b48dab6fb45666854b35   # v2.0.2
    - name: Building Python and running pyperformance
      run: |
        setarch -R python workflow_bootstrap.py ${{ inputs.fork }} ${{ inputs.ref }} \
            ${{ (inputs.machine == 'all' || inputs.machine == '__really_all') && inputs.machine || '"$BENCHMARK_RUNNER_NAME"' }} \
            ${{ inputs.benchmarks || 'all' }} ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ inputs.tailcall == true && 'tailcall' || '' }} ${{ inputs.force && '--force' || '' }} ${{ inputs.pgo && '--pgo' || '' }} ${{ inputs.perf && '--perf' || '' }} --run_id ${{ github.run_id }}
      # Pull again, since another job may have committed results in the meantime
    - name: Pull benchmarking
      if: ${{ !inputs.perf }}
      run: |
        # Another benchmarking task may have created results for the same
        # commit while the above was running. This "magic" incantation means
        # that any local results for this commit will override anything we
        # just pulled in in that case.
        git pull -s recursive -X ours --autostash --rebase
    - name: Adding data to repo
      if: ${{ !inputs.perf }}
      uses: EndBug/add-and-commit@290ea2c423ad77ca9c62ae0f5b224379612c0321   #v10.0.0
      with:
        add: results
    - name: Upload benchmark artifacts
      if: ${{ !inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: benchmark
        path: |
          benchmark.json
        overwrite: true
    - name: Upload perf artifacts
      if: ${{ inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: perf
        path: |
          profiling/results

    if: ${{ (inputs.machine == 'linux-x86_64-centurion_clang22' || 
      inputs.machine == '__really_all' || inputs.machine == 'all') }}
    env:
      flags: ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true
        && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ 
        inputs.tailcall == true && 'tailcall' || '' }}
  benchmark-linux-x86_64-centurion_gcc15:
    runs-on: [self-hosted, linux, bare-metal, centurion]
    timeout-minutes: 1440

    steps:
    - name: Setup environment
      run: |-
        echo "PYTHONHASHSEED=42" >> $GITHUB_ENV
        echo "CC=gcc-15" >> $GITHUB_ENV
        echo "CXX=g++-15" >> $GITHUB_ENV
        echo "CPU_AFFINITY=12,13,14,15,16,17,18,19" >> $GITHUB_ENV
        echo "BENCHMARK_MACHINE_NICKNAME=centurion_gcc15" >> $GITHUB_ENV
        echo "BENCHMARK_RUNNER_NAME=linux-x86_64-centurion_gcc15" >> $GITHUB_ENV
    - name: Checkout benchmarking
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd   # v6.0.2
    - name: git gc
      run: |
        git gc
    - uses: fregante/setup-git-user@024bc0b8e177d7e77203b48dab6fb45666854b35   # v2.0.2
    - name: Building Python and running pyperformance
      run: |
        setarch -R python workflow_bootstrap.py ${{ inputs.fork }} ${{ inputs.ref }} \
            ${{ (inputs.machine == 'all' || inputs.machine == '__really_all') && inputs.machine || '"$BENCHMARK_RUNNER_NAME"' }} \
            ${{ inputs.benchmarks || 'all' }} ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ inputs.tailcall == true && 'tailcall' || '' }} ${{ inputs.force && '--force' || '' }} ${{ inputs.pgo && '--pgo' || '' }} ${{ inputs.perf && '--perf' || '' }} --run_id ${{ github.run_id }}
      # Pull again, since another job may have committed results in the meantime
    - name: Pull benchmarking
      if: ${{ !inputs.perf }}
      run: |
        # Another benchmarking task may have created results for the same
        # commit while the above was running. This "magic" incantation means
        # that any local results for this commit will override anything we
        # just pulled in in that case.
        git pull -s recursive -X ours --autostash --rebase
    - name: Adding data to repo
      if: ${{ !inputs.perf }}
      uses: EndBug/add-and-commit@290ea2c423ad77ca9c62ae0f5b224379612c0321   #v10.0.0
      with:
        add: results
    - name: Upload benchmark artifacts
      if: ${{ !inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: benchmark
        path: |
          benchmark.json
        overwrite: true
    - name: Upload perf artifacts
      if: ${{ inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: perf
        path: |
          profiling/results

    if: ${{ (inputs.machine == 'linux-x86_64-centurion_gcc15' || inputs.machine 
      == '__really_all' || inputs.machine == 'all') }}
    env:
      flags: ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true
        && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ 
        inputs.tailcall == true && 'tailcall' || '' }}
  benchmark-linux-x86_64-centurion_gcc9:
    runs-on: [self-hosted, linux, bare-metal, centurion]
    timeout-minutes: 1440

    steps:
    - name: Setup environment
      run: |-
        echo "PYTHONHASHSEED=42" >> $GITHUB_ENV
        echo "CC=gcc-9" >> $GITHUB_ENV
        echo "CXX=g++-9" >> $GITHUB_ENV
        echo "CPU_AFFINITY=12,13,14,15,16,17,18,19" >> $GITHUB_ENV
        echo "BENCHMARK_MACHINE_NICKNAME=centurion_gcc9" >> $GITHUB_ENV
        echo "BENCHMARK_RUNNER_NAME=linux-x86_64-centurion_gcc9" >> $GITHUB_ENV
    - name: Checkout benchmarking
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd   # v6.0.2
    - name: git gc
      run: |
        git gc
    - uses: fregante/setup-git-user@024bc0b8e177d7e77203b48dab6fb45666854b35   # v2.0.2
    - name: Building Python and running pyperformance
      run: |
        setarch -R python workflow_bootstrap.py ${{ inputs.fork }} ${{ inputs.ref }} \
            ${{ (inputs.machine == 'all' || inputs.machine == '__really_all') && inputs.machine || '"$BENCHMARK_RUNNER_NAME"' }} \
            ${{ inputs.benchmarks || 'all' }} ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ inputs.tailcall == true && 'tailcall' || '' }} ${{ inputs.force && '--force' || '' }} ${{ inputs.pgo && '--pgo' || '' }} ${{ inputs.perf && '--perf' || '' }} --run_id ${{ github.run_id }}
      # Pull again, since another job may have committed results in the meantime
    - name: Pull benchmarking
      if: ${{ !inputs.perf }}
      run: |
        # Another benchmarking task may have created results for the same
        # commit while the above was running. This "magic" incantation means
        # that any local results for this commit will override anything we
        # just pulled in in that case.
        git pull -s recursive -X ours --autostash --rebase
    - name: Adding data to repo
      if: ${{ !inputs.perf }}
      uses: EndBug/add-and-commit@290ea2c423ad77ca9c62ae0f5b224379612c0321   #v10.0.0
      with:
        add: results
    - name: Upload benchmark artifacts
      if: ${{ !inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: benchmark
        path: |
          benchmark.json
        overwrite: true
    - name: Upload perf artifacts
      if: ${{ inputs.perf }}
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.0
      with:
        name: perf
        path: |
          profiling/results

    if: ${{ (inputs.machine == 'linux-x86_64-centurion_gcc9' || inputs.machine 
      == '__really_all' || inputs.machine == 'all') }}
    env:
      flags: ${{ inputs.tier2 == true && 'tier2' || '' }},${{ inputs.jit == true
        && 'jit' || '' }},${{ inputs.nogil == true && 'nogil' || '' }},${{ 
        inputs.tailcall == true && 'tailcall' || '' }}
